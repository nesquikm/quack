{
  "name": "quack",
  "version": "0.0.1",
  "description": "Personal memory layer for Claude Code: hooks stream session context to a local ingest server, a cheap LLM extracts entities/relations into a graph DB, and an MCP server exposes search/RAG back to Claude Code.",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.13",
  "scripts": {
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
