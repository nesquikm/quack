---
milestone: M7
status: archived
archived_at: 2026-05-15T16:11:03Z
kickoff_branch: null
frozen_at: null
kind: feature
---

# M7 — Release-process hardening

## M7: Release-process hardening {#M7}

**Goal:** Close the M6-shipped release-time gap where `package.json` advanced to v0.4.0 but the plugin marketplace metadata (`plugins/quack/.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json`) stayed at 0.2.0 — Claude Code's `/plugin` resolver still advertised v0.2.0, so installed clients never received the new `_lib/` tree shipped by FR-44QGKH.

**Prerequisites:** M6 (Plugin-hermetic hooks) shipped at v0.4.0.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| 9MMXZP | Pin plugin metadata version parity with server release | n/a |

**Tasks:**

- [x] Extend `tests/plugin-version-sync.test.ts` with three-way parity (`package.json` + `plugin.json` + `marketplace.json[quack].version`)
  verify: `bun test tests/plugin-version-sync.test.ts` fails on the current 0.4.0 / 0.2.0 drift
- [x] Bump `plugins/quack/.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` (quack entry) to 0.4.1
  verify: `grep -c '"version": "0.4.1"' plugins/quack/.claude-plugin/plugin.json .claude-plugin/marketplace.json` returns 2
- [x] Extend `CLAUDE.md ## Release Files` with two entries (json for plugin.json, regex for marketplace.json)
  verify: `grep -c 'plugins/quack/.claude-plugin/plugin.json' CLAUDE.md` returns 1
- [x] Bump `package.json` to 0.4.1 + new CHANGELOG section "Lockstep"
  verify: `node -e "console.log(require('./package.json').version)"` prints `0.4.1`
- [ ] Manual smoke: `/plugin update quack` picks up v0.4.1; installed cache contains `_lib/dispatch.ts`
  verify: `ls ~/.claude/plugins/cache/quack/quack/0.4.1/hooks/_lib/dispatch.ts`

**Tests / Gate:** `bunx tsc --noEmit && bun run test`

**Acceptance Criteria:** see `specs/frs/9MMXZP.md` (AC-9MMXZP.1..5).
