---
milestone: M1
status: archived
archived_at: 2026-05-29T05:49:03Z
kickoff_branch: null
frozen_at: null
kind: scaffolding
---

# Implementation Plan

## Milestone Order

Each milestone gets its own `specs/plan/M<N>.md` file. M1 is the bootstrap scaffold shipped pre-toolkit — its single FR is the scaffolding itself. Subsequent milestones (M2+) decide ingest server, extractor, graph DB, and MCP surface.

## M1: Bootstrap (Bun/TS scaffold + /dev-process-toolkit) {#M1}

**Goal:** Ship a green-gate Bun/TypeScript scaffold with the dev-process-toolkit conventions in place (CLAUDE.md, specs/, .claude/settings.json) so M2 can start from a working baseline.

**Prerequisites:** None

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| <scaffolding> | Bootstrap (barrel + primary feature shipped pre-toolkit) | n/a |

**Tasks:**
- [x] `git init` and scaffold Bun/TS project (package.json, tsconfig.json, .gitignore, src/index.ts, src/.placeholder.test.ts)
  verify: `bunx tsc --noEmit && bun test` exits 0 with `1 pass`
- [x] Write CLAUDE.md with Bun gate, mode:none Schema L, all-false ## Docs, ## Release Files, ## /setup audit
  verify: `grep -c '^## /setup audit$' CLAUDE.md` returns 1
- [x] Merge canonical Bun permissions into .claude/settings.json (preserving enabledPlugins)
  verify: `grep -c '"Bash(bun test)"' .claude/settings.json` returns 1
- [x] Write specs/ skeleton (requirements.md, technical-spec.md, testing-spec.md, plan/M1.md, .gitkeep stubs)
  verify: `ls specs/frs/.gitkeep specs/frs/archive/.gitkeep specs/plan/archive/.gitkeep` all exist
- [x] Bootstrap commit `chore: bootstrap dev-process-toolkit`
  verify: `git log --oneline -1` shows the bootstrap subject

**Tests:**
- The placeholder test `src/.placeholder.test.ts` keeps the gate green until M2's first real test lands.

**Acceptance Criteria:**
- [x] `bunx tsc --noEmit && bun test` passes on a freshly cloned repo
- [x] CLAUDE.md is present and committed at HEAD with the toolkit marker
- [x] `.claude/settings.json` carries the canonical Bun allowlist merged with the existing `enabledPlugins`
- [x] `specs/` tree is in place and ready for /spec-write to fill in M2

**Gate:** `bunx tsc --noEmit && bun test`

## Next milestones (deferred)

M2+ will be authored via `/brainstorm` + `/spec-write` once the open design questions are resolved (hook scope, extraction schema, graph DB choice, vector vs. graph-only retrieval, redaction policy, project boundaries, consolidation/decay, MCP surface).
