---
milestone: M2
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
---

# Implementation Plan

## Milestone Order

M1 (Bootstrap) shipped pre-toolkit; M2 is the first feature milestone — multi-tenant auth, admin MCP surface, and Docker Compose deployment, sequenced by dependency.

## M2: Multi-tenant auth + Docker deployment {#M2}

**Goal:** Quack server boots from `docker compose up`, authenticates `(user, project)` tokens on both `/ingest` and `/mcp`, and an admin can manage users/projects/tokens via MCP tools — all without the memory plane (graph DB, `search_memory`, extractor) being wired up yet.

**Prerequisites:** M1 (Bootstrap) complete — green Bun gate, `specs/` populated, `auth.sqlite` schema agreed in `technical-spec.md` §2.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| HA2WTQ | Auth middleware, auth.sqlite, and token primitives | n/a |
| WSFVNP | Admin MCP tools surface | n/a |
| BKPM28 | Docker Compose deployment | n/a |

**Tasks** (dependency-ordered; each ≈ one commit's worth of work):

- [ ] FR-HA2WTQ §1: Add `zod` dep; write `src/shared/env.ts` (Zod env schema, defaults, throws on violation).
  verify: `bun test src/shared/env.test.ts` covers happy path + 3 missing-var cases.
- [ ] FR-HA2WTQ §2: Write `src/auth/tokens.ts` (`generateToken` / `hashToken` / `verifyToken`).
  verify: AC-HA2WTQ.2 — entropy/length/round-trip/timing-safe tests pass.
- [ ] FR-HA2WTQ §3: Write `src/auth/sqlite/schema.ts` (DDL + `runMigrations`).
  verify: AC-HA2WTQ.1 — idempotent migration test asserts `_migrations` row count is 1 after two runs.
- [ ] FR-HA2WTQ §4: Write `src/auth/bootstrap.ts` (`bootstrapAdmin`).
  verify: AC-HA2WTQ.3 — first/second-boot tests + missing-env-var throws `BootstrapError`.
- [ ] FR-HA2WTQ §5: Write `src/auth/middleware.ts` (prepared statement, returns Context or null).
  verify: AC-HA2WTQ.4 — happy path + 4 failure modes return byte-identical 401 body.
- [ ] FR-HA2WTQ §6: Write `src/shared/logger.ts` (Authorization-stripping).
  verify: AC-HA2WTQ.6 — log buffer contains no plaintext token or decoded form.
- [ ] FR-HA2WTQ §7: Write `src/server/index.ts` (Bun HTTP server with `/health`, `/ingest`, `/mcp` stubs).
  verify: AC-HA2WTQ.5 — `/health` unauthenticated 200; POST routes 401/204; `127.0.0.1` bind only.
- [ ] FR-HA2WTQ §8: Add `src/auth/middleware.bench.test.ts` (1000-request p95 benchmark).
  verify: AC-HA2WTQ.7 — p95 < 5 ms on seeded DB.
- [ ] FR-WSFVNP §1: Add `@modelcontextprotocol/sdk` dep; write `src/mcp/server.ts` mounting on `/mcp`.
  verify: `bun test src/mcp/server.test.ts` — MCP HTTP client lists tools.
- [ ] FR-WSFVNP §2: Write `src/mcp/dispatch.ts` (`ADMIN_TOOLS` set + role gate).
  verify: AC-WSFVNP.1 — admin/member matrix; non-admin → 403.
- [ ] FR-WSFVNP §3: Write `src/admin/dto.ts` (explicit DTO mappers; no internal fields leak).
  verify: AC-WSFVNP.11 — list responses never contain `token_hash`.
- [ ] FR-WSFVNP §4: Write `src/admin/tools/register_user.ts`.
  verify: AC-WSFVNP.2 — plaintext + DTO; duplicate errors; non-admin forbidden.
- [ ] FR-WSFVNP §5: Write `src/admin/tools/remove_user.ts`.
  verify: AC-WSFVNP.3 — cascade; last-admin refusal; remove-self refusal.
- [ ] FR-WSFVNP §6: Write `src/admin/tools/create_project.ts` + `delete_project.ts`.
  verify: AC-WSFVNP.4 + AC-WSFVNP.5 — slug regex, reserved-slug refusal, `pending_cleanup` insertion.
- [ ] FR-WSFVNP §7: Write `src/admin/tools/add_member.ts` + `remove_member.ts`.
  verify: AC-WSFVNP.6 + AC-WSFVNP.7 — plaintext on add; revoke count on remove; last-control-admin refusal.
- [ ] FR-WSFVNP §8: Write `src/admin/tools/revoke_token.ts` + `list_projects.ts` + `list_users.ts`.
  verify: AC-WSFVNP.8 + AC-WSFVNP.9 — uniform `not_found`; member sees own only.
- [ ] FR-BKPM28 §1: Write `Dockerfile` (multi-stage, non-root, uid 1000, port 7474).
  verify: AC-BKPM28.1 + AC-BKPM28.7 + AC-BKPM28.8 — `docker build` succeeds, image < 200 MB, `id -u` returns 1000.
- [ ] FR-BKPM28 §2: Write `compose.yml` (`127.0.0.1:7474:7474`, healthcheck, daemon-graph profile placeholder).
  verify: AC-BKPM28.2 + AC-BKPM28.4 — `compose-config.test.ts` parses and asserts shape.
- [ ] FR-BKPM28 §3: Write `.env.example` + `.dockerignore`.
  verify: AC-BKPM28.3 + AC-BKPM28.7 — env vars match `src/shared/env.ts`; `.dockerignore` excludes specs.
- [ ] FR-BKPM28 §4: Append "Deployment" section to `README.md` (with marker comment for idempotency).
  verify: AC-BKPM28.5 — section present; re-run does not duplicate.
- [ ] FR-BKPM28 §5: Write `tests/docker-build.test.ts` + `tests/docker-run.test.ts` + `tests/compose-config.test.ts`.
  verify: AC-BKPM28.6 — full stack reaches healthy state within 30 s; tests skip when `docker` absent.
- [ ] M2 closeout: Update `CLAUDE.md` § Testing Conventions to note the `tests/`-mirror override for ops tests (FR-BKPM28 only); update traceability matrix in `requirements.md` §6.
  verify: `bunx tsc --noEmit && bun test` green; manual `docker compose up` smoke test.

**Tests** (cross-FR concerns):
- Boundary tests for `<memory>` wrapping are deferred to a later FR (depends on `search_memory` → graph DB).
- Auth-middleware tests (FR-HA2WTQ) are the load-bearing security tests for M2.

**Acceptance Criteria** (milestone-level):

- [ ] All ACs in FR-HA2WTQ, FR-WSFVNP, FR-BKPM28 pass.
- [ ] `bunx tsc --noEmit && bun test` green on every commit.
- [ ] `docker compose up` from a fresh clone with `QUACK_BOOTSTRAP_TOKEN` set in `.env` brings the stack to healthy state within 30 s (NFR-4).
- [ ] End-to-end manual smoke: admin token calls `register_user` via MCP, the returned plaintext token authenticates a subsequent `/ingest` POST returning 204, and a fresh non-admin token is refused by `register_user` with 403.

**Gate:** `bunx tsc --noEmit && bun test`

**Dependency graph:**

```
FR-HA2WTQ (auth foundation)
   ├─► FR-WSFVNP (admin MCP — needs server + middleware)
   └─► FR-BKPM28 (Docker — needs /health endpoint)
```

## Next milestones (deferred)

M3+ will be authored once the open graph-DB question (Kùzu / Neo4j / Memgraph / SQLite-with-edges) is resolved via `/brainstorm`. Anticipated scope: `GraphAdapter` implementation, `search_memory` MCP tool, ingest payload extraction via cheap-model API, redaction policy, `pending_cleanup` reconciliation sweep, and the cross-store project-delete cleanup job.
